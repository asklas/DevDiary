URL = github.com/[ID]/[RepositoryName]

원격 저장소 연결하기
$ git remote add origin [주소]

원격 저장소는 여러개 연결이 가능하다!
-> 별명을 다르게 지정해야한다.

$ git push -u origin main
브런치 변경내용 원격브런치로 푸쉬

-u 로 연결한 후에는?
$ git push

원격의 수정사항을 로컬에 적용하려면?
$ git pull

깃으로 협업하기
1. main 브랜치에는 커밋하지 않는다
2. main 브랜치를 기준으로 새로운 브랜치를 만든다
3. 브랜치 이름은 feature/기능 이름 으로 사용한다(팀마다 다름)
4. feature 브랜치는 한 명만 커밋한다
5. 기능 개발이 끝나면 main 브랜치와 합친다

프로필 꾸미기

잘 꾸민 깃허브 프로필 모음
https://zzetao.github.io/awesome-github-profile/

프로필 헤더 만들기
https://capsule-render.vercel.app/

